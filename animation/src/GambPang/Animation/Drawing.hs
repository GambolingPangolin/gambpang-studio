module GambPang.Animation.Drawing (
    -- * Drafting
    Drawing,
    union,
    draw,
    mask,
    exclude,
    transform,
    Shape,
    disc,
    ellipse,
    rectangle,
    polygon,

    -- * Rendering
    renderDrawing,
) where

import GambPang.Animation.Drawing.Internal
